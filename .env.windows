# Windows-specific environment configuration
# This file contains settings to prevent common Windows errors

# Force color output on Windows terminals
FORCE_COLOR=1

# Disable node warnings that can cause EOF errors
NODE_NO_WARNINGS=1

# Set proper encoding for Windows
NODE_OPTIONS=--max-old-space-size=4096 --enable-source-maps

# Prevent git EOF errors
GIT_TERMINAL_PROMPT=0

# Windows-specific Vite settings
VITE_CJS_IGNORE_WARNING=true